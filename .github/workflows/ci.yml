name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: swift test
      - name: Validate LiteLLM metadata sync script
        run: python3 Scripts/sync_litellm_metadata.py --check
